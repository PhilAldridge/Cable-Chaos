using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class cubeScript : MonoBehaviour
{
    public static bool cubeDrag = false;
    public static List<GameObject> selectedPath = new List<GameObject>();
    public static int currentIndex = -1;
    public static int pathIndex = 0;
    public static int otherPathIndex = 0;
    public static List<GameObject> selectedPipes = new List<GameObject>();
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }


}
